[pytest]
# Tests import the application modules directly (database, notifications, ...),
# so the repository root has to be on sys.path. This lets `pytest` be run from
# the project root with no PYTHONPATH juggling.
pythonpath = .
testpaths = tests
